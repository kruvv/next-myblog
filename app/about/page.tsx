import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About | Next App",
  description: "page About in next blog app",
};

export default function About() {
  return <h3>Select subitem</h3>;
}
